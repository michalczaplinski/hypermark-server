export enum ErrorNames {
  user_not_found = 'User not found',
  user_no_password = 'User does not have a password',
  incorrect_password = 'Incorrect password',
  no_jwt_secret_set = 'There is no JWT secret set',
}
